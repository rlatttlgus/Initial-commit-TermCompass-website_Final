<!doctype html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <!-- FiveServer auto-refresh 비활성화 -->
    <meta name="fiveserver-disable-auto-reload" content="true" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>TermCompass — 투자 판단의 근거를 찾다</title>
    <!-- 폰트: Pretendard (한글 본문) + Fraunces (영문 디스플레이) + JetBrains Mono (데이터) -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/gh/orioncactus/pretendard@v1.3.9/dist/web/static/pretendard.min.css"
    />
    <link
      href="https://fonts.googleapis.com/css2?family=Fraunces:opsz,wght@9..144,400;9..144,500;9..144,600;9..144,700;9..144,800;9..144,900&family=JetBrains+Mono:wght@400;500;600&display=swap"
      rel="stylesheet"
    />

    <style>
      /* ====================================================================
   DESIGN SYSTEM
   ==================================================================== */
      :root {
        /* Surfaces */
        --bg-canvas: #0a0e14;
        --bg-subtle: #0f141c;
        --bg-surface: #131923;
        --bg-elevated: #1a2030;
        --bg-tint: rgba(255, 255, 255, 0.02);

        /* Borders */
        --border-subtle: #1f2633;
        --border-default: #2a3343;
        --border-strong: #3d4658;
        --border-accent: rgba(14, 165, 233, 0.4);

        /* Foreground */
        --fg-primary: #f4f1ea;
        --fg-secondary: #b8bdc7;
        --fg-muted: #6b7280;
        --fg-faint: #4b5563;

        /* Brand Accent - Cyan (replaced gold) */
        --accent: #0ea5e9;
        --accent-bright: #06b6d4;
        --accent-dim: #0369a1;

        /* Stock data (Korean convention) */
        --up: #ff4d4d;
        --down: #4d8eff;
        --warning: #f5b342;
        --info: #5fb3d4;
        --neutral: #9ca3af;

        /* Type */
        --font-display: "Fraunces", "Pretendard", Georgia, serif;
        --font-body: "Pretendard", -apple-system, sans-serif;
        --font-mono: "JetBrains Mono", ui-monospace, monospace;

        /* Layout */
        --max: 1280px;
        --gutter: clamp(20px, 5vw, 64px);
      }

      * {
        box-sizing: border-box;
      }
      *::selection {
        background: var(--accent);
        color: var(--bg-canvas);
      }

      html {
        scroll-behavior: smooth;
      }

      body {
        margin: 0;
        background: var(--bg-canvas);
        color: var(--fg-primary);
        font-family: var(--font-body);
        font-weight: 400;
        font-size: 16px;
        line-height: 1.6;
        letter-spacing: -0.2px;
        overflow-x: hidden;
      }

      /* 배경은 부드러운 라디얼 글로우 2개만 */
      body::before {
        content: "";
        position: fixed;
        inset: 0;
        background:
          radial-gradient(
            1400px 800px at 85% -10%,
            rgba(14, 165, 233, 0.1),
            transparent 60%
          ),
          radial-gradient(
            1000px 800px at -10% 100%,
            rgba(77, 142, 255, 0.07),
            transparent 60%
          ),
          radial-gradient(
            800px 500px at 50% 50%,
            rgba(14, 165, 233, 0.02),
            transparent 70%
          );
        pointer-events: none;
        z-index: 0;
      }

      /* ====================================================================
   TOP NAV
   ==================================================================== */
      .nav {
        position: sticky;
        top: 0;
        z-index: 50;
        background: rgba(10, 14, 20, 0.85);
        backdrop-filter: blur(20px) saturate(140%);
        -webkit-backdrop-filter: blur(20px) saturate(140%);
        border-bottom: 1px solid var(--border-subtle);
      }
      .nav-inner {
        max-width: var(--max);
        margin: 0 auto;
        padding: 16px var(--gutter);
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 24px;
      }
      .brand {
        display: flex;
        align-items: center;
        gap: 12px;
        font-weight: 700;
        letter-spacing: -0.3px;
      }
      .brand-mark {
        width: 32px;
        height: 32px;
        background: var(--accent);
        color: var(--bg-canvas);
        display: grid;
        place-items: center;
        font-family: var(--font-display);
        font-weight: 900;
        font-size: 16px;
        border-radius: 4px;
      }
      .brand-text {
        font-family: var(--font-display);
        font-weight: 700;
        font-size: 17px;
        letter-spacing: -0.5px;
      }
      .brand-text em {
        font-style: normal;
        color: var(--accent);
        font-weight: 700;
      }
      .nav-meta {
        font-family: var(--font-mono);
        font-size: 12px;
        color: var(--fg-muted);
        letter-spacing: 0.5px;
        display: flex;
        gap: 16px;
        align-items: center;
      }
      .nav-meta span:first-child::before {
        content: "";
        display: inline-block;
        width: 6px;
        height: 6px;
        border-radius: 50%;
        background: var(--up);
        margin-right: 8px;
        vertical-align: middle;
        animation: pulse 2s infinite;
      }
      @keyframes pulse {
        0%,
        100% {
          opacity: 1;
        }
        50% {
          opacity: 0.3;
        }
      }

      /* ====================================================================
   ANIMATIONS
   ==================================================================== */
      @keyframes fadeUp {
        from {
          opacity: 0;
          transform: translateY(24px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }
      @keyframes ticker {
        from {
          transform: translateX(0);
        }
        to {
          transform: translateX(-50%);
        }
      }
      .reveal {
        opacity: 0;
        animation: fadeUp 0.9s cubic-bezier(0.16, 1, 0.3, 1) forwards;
      }
      .d-1 {
        animation-delay: 0.1s;
      }
      .d-2 {
        animation-delay: 0.25s;
      }
      .d-3 {
        animation-delay: 0.4s;
      }
      .d-4 {
        animation-delay: 0.55s;
      }
      .d-5 {
        animation-delay: 0.7s;
      }

      /* ====================================================================
   TICKER TAPE (최적화)
   ==================================================================== */
      .ticker {
        border-bottom: 1px solid var(--border-subtle);
        background: var(--bg-subtle);
        overflow: hidden;
        white-space: nowrap;
        font-family: var(--font-mono);
        font-size: 12px;
        position: relative;
      }
      .ticker-track {
        display: inline-block;
        animation: ticker 60s linear infinite;
        padding: 10px 0;
        will-change: transform;
        backface-visibility: hidden;
        -webkit-backface-visibility: hidden;
        perspective: 1000px;
        -webkit-perspective: 1000px;
        transform: translate3d(0, 0, 0);
        -webkit-transform: translate3d(0, 0, 0);
      }
      .ticker-item {
        display: inline-block;
        padding: 0 32px;
        color: var(--fg-secondary);
      }
      .ticker-item .symbol {
        color: var(--fg-primary);
        font-weight: 500;
        margin-right: 8px;
      }
      .ticker-item .up {
        color: var(--up);
      }
      .ticker-item .down {
        color: var(--down);
      }

      /* ====================================================================
   HERO
   ==================================================================== */
      .hero {
        position: relative;
        max-width: var(--max);
        margin: 0 auto;
        padding: 96px var(--gutter) 80px;
        z-index: 1;
      }
      .eyebrow {
        display: inline-flex;
        align-items: center;
        gap: 10px;
        font-family: var(--font-mono);
        font-size: 11px;
        letter-spacing: 2px;
        text-transform: uppercase;
        color: var(--accent);
        padding: 6px 12px;
        border: 1px solid var(--border-accent);
        border-radius: 100px;
        margin-bottom: 32px;
      }
      .eyebrow::before {
        content: "";
        width: 5px;
        height: 5px;
        background: var(--accent);
        border-radius: 50%;
      }

      /* 제목: 가변 타이포그래피 */
      .hero h1 {
        font-family: var(--font-display);
        line-height: 1.05;
        margin: 0 0 28px;
        max-width: 18ch;
        color: var(--fg-primary);
        letter-spacing: -2px;
      }
      .hero h1 .small {
        font-weight: 600;
        font-size: 0.65em;
        color: var(--fg-secondary);
      }
      .hero h1 .highlight {
        display: inline-block;
        font-weight: 900;
        font-size: 1.35em;
        color: var(--accent);
        margin: 0 0;
      }
      .hero h1 .medium {
        font-weight: 600;
        font-size: 0.8em;
        color: var(--fg-secondary);
      }
      .hero h1 .kr {
        font-family: var(--font-body);
        letter-spacing: -2.5px;
      }

      .hero-sub {
        font-size: clamp(17px, 1.4vw, 20px);
        line-height: 1.7;
        color: var(--fg-secondary);
        max-width: 56ch;
        margin: 0 0 56px;
        font-weight: 400;
      }
      .hero-sub strong {
        color: var(--fg-primary);
        font-weight: 600;
      }

      /* Stats row */
      .stats {
        display: grid;
        grid-template-columns: repeat(4, 1fr);
        gap: 0;
        border-top: 1px solid var(--border-default);
        border-bottom: 1px solid var(--border-default);
        margin-bottom: 48px;
      }
      .stat {
        padding: 36px 32px;
        border-right: 1px solid var(--border-subtle);
      }
      .stat:last-child {
        border-right: none;
      }
      .stat-label {
        font-family: var(--font-mono);
        font-size: 11px;
        text-transform: uppercase;
        letter-spacing: 1.5px;
        color: var(--fg-muted);
        margin-bottom: 16px;
      }
      .stat-value {
        font-family: var(--font-display);
        font-weight: 700;
        font-size: clamp(36px, 4vw, 52px);
        line-height: 1;
        letter-spacing: -1.5px;
        color: var(--fg-primary);
        display: flex;
        align-items: baseline;
        gap: 6px;
      }
      .stat-value .unit {
        font-family: var(--font-body);
        font-size: 14px;
        color: var(--fg-muted);
        font-weight: 400;
      }
      .stat-value .accent {
        color: var(--accent);
        font-weight: 800;
      }

      .cta-row {
        display: flex;
        gap: 16px;
        flex-wrap: wrap;
        align-items: center;
      }
      .btn-primary {
        display: inline-flex;
        align-items: center;
        gap: 12px;
        background: var(--fg-primary);
        color: var(--bg-canvas);
        border: none;
        padding: 18px 28px;
        border-radius: 6px;
        font-family: var(--font-body);
        font-weight: 700;
        font-size: 15px;
        letter-spacing: -0.3px;
        cursor: pointer;
        transition: all 0.2s ease;
      }
      .btn-primary:hover {
        background: var(--accent);
        transform: translateY(-2px);
        box-shadow: 0 12px 32px rgba(14, 165, 233, 0.25);
      }
      .btn-primary svg {
        transition: transform 0.2s;
      }
      .btn-primary:hover svg {
        transform: translateX(4px);
      }

      .btn-ghost {
        display: inline-flex;
        align-items: center;
        gap: 8px;
        background: transparent;
        color: var(--fg-secondary);
        border: 1px solid var(--border-default);
        padding: 17px 24px;
        border-radius: 6px;
        font-family: var(--font-body);
        font-weight: 500;
        font-size: 14px;
        cursor: pointer;
        transition: all 0.2s ease;
        text-decoration: none;
      }
      .btn-ghost:hover {
        border-color: var(--border-strong);
        color: var(--fg-primary);
      }

      /* ====================================================================
   SECTION COMMON
   ==================================================================== */
      section {
        position: relative;
        z-index: 1;
      }
      .section-inner {
        max-width: var(--max);
        margin: 0 auto;
        padding: 80px var(--gutter);
      }
      .section-header {
        display: flex;
        align-items: flex-end;
        justify-content: space-between;
        gap: 32px;
        margin-bottom: 56px;
        border-bottom: 1px solid var(--border-default);
        padding-bottom: 32px;
      }
      .section-header-left {
        flex: 1;
      }
      .section-num {
        font-family: var(--font-mono);
        font-size: 12px;
        color: var(--accent);
        letter-spacing: 2px;
        margin-bottom: 16px;
        display: block;
      }
      .section-title {
        font-family: var(--font-display);
        font-weight: 700;
        font-size: clamp(32px, 4vw, 48px);
        line-height: 1.1;
        letter-spacing: -1.5px;
        margin: 0;
      }
      .section-title em {
        font-style: normal;
        color: var(--accent);
        font-weight: 800;
      }
      .section-title .kr {
        font-family: var(--font-body);
        font-weight: 800;
        letter-spacing: -2px;
      }
      .section-desc {
        max-width: 32ch;
        color: var(--fg-secondary);
        font-size: 15px;
        line-height: 1.6;
        flex-shrink: 0;
      }

      /* ====================================================================
   PERSONA SECTION
   ==================================================================== */
      .persona-grid {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 0;
        border-top: 1px solid var(--border-subtle);
      }
      .persona-card {
        padding: 40px 32px 40px 0;
        border-right: 1px solid var(--border-subtle);
        border-bottom: 1px solid var(--border-subtle);
      }
      .persona-card:nth-child(3n) {
        border-right: none;
      }
      .persona-card:nth-child(3n + 2),
      .persona-card:nth-child(3n + 3) {
        padding-left: 32px;
      }

      .persona-num {
        font-family: var(--font-display);
        font-style: normal;
        font-weight: 700;
        font-size: 64px;
        color: #0369a1;
        line-height: 1;
        margin-bottom: 24px;
        letter-spacing: -2px;
        opacity: 0.6;
      }
      .persona-card h3 {
        font-family: var(--font-body);
        font-weight: 700;
        font-size: 22px;
        margin: 0 0 16px;
        color: var(--fg-primary);
        letter-spacing: -0.5px;
      }
      .persona-card p {
        margin: 0 0 24px;
        color: var(--fg-secondary);
        font-size: 15px;
        line-height: 1.7;
      }
      .persona-not {
        border-top: 1px dashed var(--border-default);
        padding-top: 16px;
        font-size: 13px;
        color: var(--fg-muted);
        font-family: var(--font-mono);
        line-height: 1.6;
      }
      .persona-not::before {
        content: "✕ ";
        color: var(--up);
        font-weight: 600;
      }

      /* ====================================================================
   RESPONSE FORMAT (CHAT MOCKUP) - 핵심 섹션
   ==================================================================== */
      .chat-section {
        background: var(--bg-subtle);
        border-top: 1px solid var(--border-subtle);
        border-bottom: 1px solid var(--border-subtle);
      }
      .chat-demo {
        display: grid;
        grid-template-columns: 1fr 1.2fr;
        gap: 64px;
        align-items: start;
      }
      .chat-demo-left h3 {
        font-family: var(--font-display);
        font-weight: 700;
        font-size: 28px;
        line-height: 1.3;
        margin: 0 0 24px;
        letter-spacing: -1px;
      }
      .chat-demo-left h3 em {
        color: var(--accent);
        font-style: normal;
        font-weight: 800;
      }
      .chat-demo-left p {
        color: var(--fg-secondary);
        font-size: 15px;
        line-height: 1.7;
        margin-bottom: 32px;
      }
      .structure-list {
        list-style: none;
        padding: 0;
        margin: 0;
        border-top: 1px solid var(--border-default);
      }
      .structure-list li {
        display: flex;
        align-items: center;
        gap: 16px;
        padding: 14px 0;
        border-bottom: 1px solid var(--border-subtle);
        font-size: 14px;
      }
      .structure-list .step {
        font-family: var(--font-mono);
        font-size: 11px;
        color: var(--fg-muted);
        width: 28px;
      }
      .structure-list .icon {
        width: 28px;
        text-align: center;
        font-size: 16px;
      }
      .structure-list .label {
        font-weight: 600;
        color: var(--fg-primary);
        flex: 1;
      }
      .structure-list .hint {
        color: var(--fg-muted);
        font-size: 12px;
        font-family: var(--font-mono);
      }

      /* Chat bubble mockup */
      .chat-window {
        background: var(--bg-surface);
        border: 1px solid var(--border-default);
        border-radius: 12px;
        overflow: hidden;
        box-shadow: 0 24px 60px rgba(0, 0, 0, 0.4);
      }
      .chat-header {
        background: var(--bg-elevated);
        padding: 14px 20px;
        border-bottom: 1px solid var(--border-subtle);
        display: flex;
        align-items: center;
        gap: 12px;
        font-family: var(--font-mono);
        font-size: 12px;
        color: var(--fg-muted);
      }
      .chat-header .dots {
        display: flex;
        gap: 6px;
      }
      .chat-header .dots span {
        width: 10px;
        height: 10px;
        border-radius: 50%;
        background: var(--border-strong);
      }
      .chat-header .dots span:nth-child(1) {
        background: #ff5f57;
      }
      .chat-header .dots span:nth-child(2) {
        background: #ffbd2e;
      }
      .chat-header .dots span:nth-child(3) {
        background: #28c940;
      }
      .chat-body {
        padding: 24px;
        max-height: 600px;
        overflow-y: auto;
      }
      .user-msg {
        background: var(--bg-elevated);
        border: 1px solid var(--border-subtle);
        border-radius: 10px;
        padding: 14px 18px;
        margin-bottom: 20px;
        font-size: 14px;
        color: var(--fg-primary);
        position: relative;
      }
      .user-msg::before {
        content: "YOU";
        position: absolute;
        top: -8px;
        left: 14px;
        background: var(--bg-elevated);
        padding: 0 6px;
        font-family: var(--font-mono);
        font-size: 9px;
        letter-spacing: 1px;
        color: var(--fg-muted);
      }
      .bot-msg {
        font-size: 13.5px;
        line-height: 1.7;
        color: var(--fg-secondary);
      }
      .bot-section {
        margin-bottom: 16px;
        padding-bottom: 16px;
        border-bottom: 1px solid var(--border-subtle);
      }
      .bot-section:last-child {
        border-bottom: none;
        margin-bottom: 0;
        padding-bottom: 0;
      }
      .bot-section-title {
        font-weight: 700;
        color: var(--fg-primary);
        margin-bottom: 6px;
        display: flex;
        align-items: center;
        gap: 8px;
        font-size: 13.5px;
      }
      .bot-section .formula {
        font-family: var(--font-mono);
        font-size: 12px;
        background: var(--bg-elevated);
        padding: 6px 10px;
        border-radius: 4px;
        display: inline-block;
        color: var(--accent-bright);
      }
      .bot-section ul {
        margin: 6px 0 0;
        padding-left: 18px;
      }
      .bot-section ul li {
        margin-bottom: 4px;
      }
      .bot-section .num {
        color: var(--up);
        font-family: var(--font-mono);
        font-weight: 600;
      }
      .bot-section .num-blue {
        color: var(--down);
        font-family: var(--font-mono);
        font-weight: 600;
      }
      .bot-followup {
        margin-top: 8px;
        display: flex;
        flex-direction: column;
        gap: 6px;
      }
      .bot-followup a {
        display: block;
        padding: 8px 12px;
        background: var(--bg-elevated);
        border: 1px solid var(--border-subtle);
        border-radius: 6px;
        color: var(--fg-secondary);
        font-size: 12.5px;
        text-decoration: none;
        transition: all 0.2s;
      }
      .bot-followup a::before {
        content: "→ ";
        color: var(--accent);
      }
      .bot-followup a:hover {
        color: var(--fg-primary);
        border-color: var(--border-accent);
      }

      /* ====================================================================
   DATABASE SECTION
   ==================================================================== */
      .db-grid {
        display: grid;
        grid-template-columns: 1.1fr 1fr;
        gap: 64px;
        align-items: start;
      }
      .db-categories {
        background: var(--bg-surface);
        border: 1px solid var(--border-default);
        border-radius: 8px;
        padding: 32px;
      }
      .db-categories h4 {
        font-family: var(--font-mono);
        font-size: 11px;
        text-transform: uppercase;
        letter-spacing: 2px;
        color: var(--fg-muted);
        margin: 0 0 24px;
      }
      .cat-row {
        display: grid;
        grid-template-columns: 1fr 60px 80px;
        gap: 16px;
        align-items: center;
        padding: 12px 0;
        border-bottom: 1px solid var(--border-subtle);
        font-size: 14px;
      }
      .cat-row:last-child {
        border-bottom: none;
      }
      .cat-name {
        color: var(--fg-primary);
        font-weight: 500;
      }
      .cat-bar {
        height: 4px;
        background: var(--bg-elevated);
        border-radius: 2px;
        overflow: hidden;
        position: relative;
      }
      .cat-bar-fill {
        height: 100%;
        background: linear-gradient(90deg, #0369a1, var(--accent));
        border-radius: 2px;
        width: 0;
        animation: barFill 1.5s cubic-bezier(0.16, 1, 0.3, 1) forwards;
      }
      @keyframes barFill {
        to {
          width: var(--w, 100%);
        }
      }
      .cat-count {
        font-family: var(--font-mono);
        font-size: 13px;
        color: var(--accent);
        text-align: right;
        font-weight: 500;
      }

      .db-columns h4 {
        font-family: var(--font-mono);
        font-size: 11px;
        text-transform: uppercase;
        letter-spacing: 2px;
        color: var(--fg-muted);
        margin: 0 0 24px;
      }
      .col-item {
        padding: 16px 0;
        border-bottom: 1px solid var(--border-subtle);
        display: grid;
        grid-template-columns: 200px 1fr;
        gap: 20px;
        align-items: start;
      }
      .col-key {
        font-family: var(--font-mono);
        font-size: 13px;
        color: var(--accent);
        font-weight: 500;
      }
      .col-key .badge-new {
        display: inline-block;
        background: var(--accent);
        color: var(--bg-canvas);
        font-family: var(--font-body);
        font-size: 9px;
        font-weight: 700;
        padding: 2px 5px;
        border-radius: 3px;
        margin-left: 6px;
        letter-spacing: 0.5px;
        vertical-align: 1px;
      }
      .col-desc {
        font-size: 13.5px;
        color: var(--fg-secondary);
        line-height: 1.6;
      }

      /* ====================================================================
   SAMPLE QUESTIONS
   ==================================================================== */
      .q-grid {
        display: grid;
        grid-template-columns: repeat(2, 1fr);
        gap: 0;
        border-top: 1px solid var(--border-subtle);
        border-left: 1px solid var(--border-subtle);
      }
      .q-card {
        padding: 32px;
        border-right: 1px solid var(--border-subtle);
        border-bottom: 1px solid var(--border-subtle);
        cursor: pointer;
        transition: background 0.2s;
        position: relative;
        background: var(--bg-canvas);
      }
      .q-card:hover {
        background: var(--bg-surface);
      }
      .q-card:hover .q-arrow {
        transform: translate(4px, -4px);
        color: var(--accent);
      }
      .q-tag {
        font-family: var(--font-mono);
        font-size: 10px;
        text-transform: uppercase;
        letter-spacing: 1.5px;
        color: var(--accent);
        margin-bottom: 16px;
      }
      .q-text {
        font-size: 17px;
        line-height: 1.5;
        color: var(--fg-primary);
        font-weight: 500;
        letter-spacing: -0.3px;
        padding-right: 32px;
      }
      .q-arrow {
        position: absolute;
        top: 32px;
        right: 32px;
        color: var(--fg-muted);
        transition: all 0.2s;
        font-family: var(--font-mono);
        font-size: 20px;
      }

      /* ====================================================================
   HONESTY / FAQ
   ==================================================================== */
      .honesty-section {
        background: var(--bg-subtle);
        border-top: 1px solid var(--border-subtle);
      }
      .faq-list {
        max-width: 880px;
        margin: 0 auto;
      }
      .faq-item {
        border-bottom: 1px solid var(--border-default);
        padding: 28px 0;
        display: grid;
        grid-template-columns: 1fr 2fr;
        gap: 48px;
        align-items: start;
      }
      .faq-q {
        font-family: var(--font-body);
        font-weight: 700;
        font-size: 22px;
        line-height: 1.4;
        color: var(--fg-primary);
        letter-spacing: -0.5px;
      }
      .faq-a {
        font-size: 15px;
        line-height: 1.7;
        color: var(--fg-secondary);
      }
      .faq-a strong {
        color: var(--fg-primary);
        font-weight: 600;
      }
      .faq-a .label-no {
        display: inline-block;
        background: rgba(255, 77, 77, 0.15);
        color: var(--up);
        font-size: 11px;
        font-weight: 700;
        letter-spacing: 0.5px;
        padding: 2px 8px;
        border-radius: 3px;
        margin-right: 8px;
        font-family: var(--font-mono);
      }
      .faq-a .label-yes {
        display: inline-block;
        background: rgba(14, 165, 233, 0.15);
        color: var(--accent);
        font-size: 11px;
        font-weight: 700;
        letter-spacing: 0.5px;
        padding: 2px 8px;
        border-radius: 3px;
        margin-right: 8px;
        font-family: var(--font-mono);
      }

      /* ====================================================================
   FINAL CTA
   ==================================================================== */
      .final-cta {
        padding: 120px var(--gutter);
        text-align: center;
        max-width: var(--max);
        margin: 0 auto;
      }
      .final-cta h2 {
        font-family: var(--font-display);
        font-weight: 700;
        font-size: clamp(36px, 5vw, 60px);
        margin: 0 0 24px;
        line-height: 1.15;
        letter-spacing: -1.5px;
      }
      .final-cta h2 em {
        font-style: normal;
        color: var(--accent);
        font-weight: 800;
      }
      .final-cta h2 .kr {
        font-family: var(--font-body);
        font-weight: 800;
        letter-spacing: -2px;
      }
      .final-cta p {
        color: var(--fg-secondary);
        font-size: 17px;
        margin: 0 0 40px;
      }

      /* ====================================================================
   FOOTER
   ==================================================================== */
      footer {
        border-top: 1px solid var(--border-subtle);
        padding: 40px var(--gutter);
        max-width: var(--max);
        margin: 0 auto;
        display: flex;
        justify-content: space-between;
        align-items: center;
        font-family: var(--font-mono);
        font-size: 12px;
        color: var(--fg-muted);
      }
      footer .footer-right {
        display: flex;
        gap: 24px;
      }

      /* ====================================================================
   FLOATING TOGGLE & SIDE PANEL (이전 기능 유지)
   ==================================================================== */
      .side-panel {
        position: fixed;
        top: 0;
        right: 0;
        width: 460px;
        min-width: 380px;
        max-width: 800px;
        height: 100vh;
        background: var(--bg-canvas);
        box-shadow: -8px 0 40px rgba(0, 0, 0, 0.6);
        z-index: 9999;
        display: flex;
        transform: translateX(100%);
        transition: transform 0.5s cubic-bezier(0.16, 1, 0.3, 1);
      }
      .side-panel.open {
        transform: translateX(0);
      }
      .side-panel iframe {
        flex: 1;
        border: none;
        background: #ffffff;
      }
      .resizer {
        width: 6px;
        background-color: var(--bg-elevated);
        cursor: col-resize;
        display: flex;
        align-items: center;
        justify-content: center;
        border-right: 1px solid var(--border-default);
      }
      .resizer:hover {
        background: var(--accent);
      }
      .resizer::after {
        content: "";
        width: 2px;
        height: 32px;
        background: var(--border-strong);
        border-radius: 2px;
      }

      .toggle-btn {
        position: fixed;
        right: 32px;
        bottom: 32px;
        z-index: 10000;
        padding: 0 28px;
        height: 60px;
        border-radius: 100px;
        background: var(--fg-primary);
        color: var(--bg-canvas);
        border: none;
        font-family: var(--font-body);
        font-size: 15px;
        font-weight: 700;
        letter-spacing: -0.3px;
        cursor: pointer;
        box-shadow:
          0 12px 32px rgba(244, 241, 234, 0.15),
          0 0 0 1px rgba(14, 165, 233, 0.3);
        transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
        display: flex;
        align-items: center;
        gap: 10px;
      }
      .toggle-btn:hover {
        background: var(--accent);
        transform: translateY(-3px);
        box-shadow: 0 16px 40px rgba(14, 165, 233, 0.35);
      }
      .toggle-btn-icon {
        width: 8px;
        height: 8px;
        background: var(--up);
        border-radius: 50%;
        animation: pulse 2s infinite;
      }

      body.panel-open .toggle-btn {
        right: calc(var(--panel-width, 460px) + 24px);
        background: var(--bg-elevated);
        color: var(--fg-primary);
        border: 1px solid var(--border-strong);
      }
      body.panel-open .toggle-btn:hover {
        background: var(--up);
        color: white;
      }

      /* ====================================================================
   RESPONSIVE
   ==================================================================== */
      @media (max-width: 960px) {
        .stats {
          grid-template-columns: repeat(2, 1fr);
        }
        .stat {
          border-right: none;
          border-bottom: 1px solid var(--border-subtle);
        }
        .stat:nth-child(odd) {
          border-right: 1px solid var(--border-subtle);
        }

        .persona-grid,
        .db-grid,
        .chat-demo {
          grid-template-columns: 1fr;
          gap: 32px;
        }
        .persona-card {
          border-right: none !important;
          padding: 32px 0 !important;
        }
        .q-grid {
          grid-template-columns: 1fr;
        }
        .faq-item {
          grid-template-columns: 1fr;
          gap: 12px;
        }
        .section-header {
          flex-direction: column;
          align-items: flex-start;
        }
      }

      @media (max-width: 640px) {
        .hero {
          padding: 64px var(--gutter) 48px;
        }
        .section-inner {
          padding: 56px var(--gutter);
        }
        .side-panel {
          width: 100% !important;
          min-width: 100%;
        }
        .resizer {
          display: none;
        }
        body.panel-open .toggle-btn {
          right: 20px;
          bottom: 20px;
          z-index: 10001;
        }
        .nav-meta {
          display: none;
        }
        .col-item {
          grid-template-columns: 1fr;
          gap: 6px;
        }
      }
    </style>
  </head>

  <body>
    <!-- ==================== TOP NAV ==================== -->
    <nav class="nav">
      <div class="nav-inner">
        <div class="brand">
          <div class="brand-mark">T</div>
          <div class="brand-text">TermCompass</div>
        </div>
        <div class="nav-meta">
          <span>LIVE</span>
          <span>디지털콘텐츠디자인학과 · 60221267 · 김시현</span>
        </div>
      </div>
    </nav>

    <!-- ==================== TICKER TAPE ==================== -->
    <div class="ticker">
      <div class="ticker-track">
        <span class="ticker-item"
          ><span class="symbol">KOSPI</span> 2,847.21
          <span class="up">▲ 18.42</span></span
        >
        <span class="ticker-item"
          ><span class="symbol">KOSDAQ</span> 856.33
          <span class="down">▼ 4.17</span></span
        >
        <span class="ticker-item"
          ><span class="symbol">005930</span> 삼성전자
          <span class="up">▲ 1.2%</span></span
        >
        <span class="ticker-item"
          ><span class="symbol">000660</span> SK하이닉스
          <span class="up">▲ 2.4%</span></span
        >
        <span class="ticker-item"
          ><span class="symbol">PER</span> 시장평균
          <span class="symbol">10.8×</span></span
        >
        <span class="ticker-item"
          ><span class="symbol">PBR</span> 시장평균
          <span class="symbol">0.94×</span></span
        >
        <span class="ticker-item"
          ><span class="symbol">기준금리</span>
          <span class="symbol">3.50%</span></span
        >
        <span class="ticker-item"
          ><span class="symbol">USD/KRW</span> 1,378.40
          <span class="up">▲ 2.10</span></span
        >
        <span class="ticker-item"
          ><span class="symbol">KOSPI</span> 2,847.21
          <span class="up">▲ 18.42</span></span
        >
        <span class="ticker-item"
          ><span class="symbol">KOSDAQ</span> 856.33
          <span class="down">▼ 4.17</span></span
        >
        <span class="ticker-item"
          ><span class="symbol">005930</span> 삼성전자
          <span class="up">▲ 1.2%</span></span
        >
        <span class="ticker-item"
          ><span class="symbol">000660</span> SK하이닉스
          <span class="up">▲ 2.4%</span></span
        >
        <span class="ticker-item"
          ><span class="symbol">PER</span> 시장평균
          <span class="symbol">10.8×</span></span
        >
        <span class="ticker-item"
          ><span class="symbol">PBR</span> 시장평균
          <span class="symbol">0.94×</span></span
        >
        <span class="ticker-item"
          ><span class="symbol">기준금리</span>
          <span class="symbol">3.50%</span></span
        >
        <span class="ticker-item"
          ><span class="symbol">USD/KRW</span> 1,378.40
          <span class="up">▲ 2.10</span></span
        >
      </div>
    </div>

    <!-- ==================== HERO ==================== -->
    <section class="hero">
      <div class="eyebrow reveal">중급 투자자를 위한 판단 도구</div>
      <h1 class="reveal d-1">
        <span class="kr small">용어 사전이 아닌,</span><br />
        <span class="highlight">investment judgment</span>
      </h1>
      <p class="hero-sub reveal d-2">
        PER, PBR, 영업이익률이 무엇인지는 알지만
        <strong>실제 매수·관망 판단에 어떻게 써야 할지</strong> 막막한 중급
        투자자를 위한 AI 어시스턴트. 검증된 144개 용어 데이터베이스를 바탕으로,
        전문 애널리스트 톤으로 답변합니다.
      </p>

      <div class="stats reveal d-3">
        <div class="stat">
          <div class="stat-label">Terms</div>
          <div class="stat-value">144 <span class="unit">개 용어</span></div>
        </div>
        <div class="stat">
          <div class="stat-label">Categories</div>
          <div class="stat-value">8 <span class="unit">분류</span></div>
        </div>
        <div class="stat">
          <div class="stat-label">Data Fields</div>
          <div class="stat-value">20 <span class="unit">컬럼</span></div>
        </div>
        <div class="stat">
          <div class="stat-label">Response Sections</div>
          <div class="stat-value">
            <span class="accent">8</span> <span class="unit">구조화 출력</span>
          </div>
        </div>
      </div>

      <div class="cta-row reveal d-4">
        <button class="btn-primary" onclick="openPanel()">
          챗봇 시작하기
          <svg width="16" height="16" viewBox="0 0 16 16" fill="none">
            <path
              d="M6 3l5 5-5 5"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"
            />
          </svg>
        </button>
        <a href="#chat-demo" class="btn-ghost">응답 예시 먼저 보기</a>
      </div>
    </section>

    <!-- ==================== WHO IS THIS FOR ==================== -->
    <section>
      <div class="section-inner">
        <div class="section-header reveal">
          <div class="section-header-left">
            <span class="section-num">01 / WHO</span>
            <h2 class="section-title">
              <em>For</em> <span class="kr">이런 분께</span>
            </h2>
          </div>
          <p class="section-desc">
            친근한 입문서가 아닙니다. 본격적인 투자 판단으로 한 발 나아가려는
            분께 맞춰진 도구입니다.
          </p>
        </div>

        <div class="persona-grid">
          <div class="persona-card">
            <div class="persona-num">01</div>
            <h3>본질을 알고 싶은 사람</h3>
            <p>
              "PER이 낮으면 좋다"는 1차 정보는 알지만,
              <strong style="color: var(--fg-primary)"
                >왜 낮은지, 무엇이 함정인지</strong
              >까지 짚고 싶은 분.
            </p>
            <div class="persona-not">시장 입문자에겐 다른 도구를</div>
          </div>
          <div class="persona-card">
            <div class="persona-num">02</div>
            <h3>호가창 너머를 보는 사람</h3>
            <p>
              차트의 모양보다
              <strong style="color: var(--fg-primary)"
                >기업가치·재무·거시지표의 의미</strong
              >로 매매 근거를 만들고 싶은 분.
            </p>
            <div class="persona-not">단기 시그널 매매자는 차트 서비스로</div>
          </div>
          <div class="persona-card">
            <div class="persona-num">03</div>
            <h3>실전 적용까지 원하는 사람</h3>
            <p>
              정의만 듣고 끝나는 게 아니라
              <strong style="color: var(--fg-primary)"
                >"몇 배 이하면 저평가인지" 수치 기준</strong
              >까지 받고 싶은 분.
            </p>
            <div class="persona-not">개별 종목 매수 추천은 제공하지 않음</div>
          </div>
        </div>
      </div>
    </section>

    <!-- ==================== HOW IT RESPONDS ==================== -->
    <section class="chat-section" id="chat-demo">
      <div class="section-inner">
        <div class="section-header">
          <div class="section-header-left">
            <span class="section-num">02 / HOW</span>
            <h2 class="section-title">
              <em>Structured</em><br /><span class="kr">이렇게 답변합니다</span>
            </h2>
          </div>
          <p class="section-desc">
            모든 답변은 8개 섹션으로 정형화됩니다. 핵심부터, 함정까지, 후속
            질문까지.
          </p>
        </div>

        <div class="chat-demo">
          <div class="chat-demo-left">
            <h3>모호한 설명 대신,<br /><em>완결된 8단계 구조</em>로.</h3>
            <p>
              "PER이 낮으면 사도 되나요?" 같은 적용 단계의 질문에,
              정의·수식·활용 기준·위험·비교·전문가 팁·종목 예시·후속 질문까지 한
              번에 정리해드립니다.
            </p>

            <ul class="structure-list">
              <li>
                <span class="step">01</span><span class="icon">📌</span
                ><span class="label">정의 + 비유</span
                ><span class="hint">definition_easy</span>
              </li>
              <li>
                <span class="step">02</span><span class="icon">🧮</span
                ><span class="label">수식</span
                ><span class="hint">formula</span>
              </li>
              <li>
                <span class="step">03</span><span class="icon">💡</span
                ><span class="label">투자 활용법 + 수치 기준</span
                ><span class="hint">investment_hint</span>
              </li>
              <li>
                <span class="step">04</span><span class="icon">⚠️</span
                ><span class="label">위험 경고 + 함정</span
                ><span class="hint">risk_warning</span>
              </li>
              <li>
                <span class="step">05</span><span class="icon">🔄</span
                ><span class="label">관련 지표 비교</span
                ><span class="hint">comparison_with</span>
              </li>
              <li>
                <span class="step">06</span><span class="icon">🎯</span
                ><span class="label">전문가 팁</span
                ><span class="hint">expert_tips</span>
              </li>
              <li>
                <span class="step">07</span><span class="icon">🏢</span
                ><span class="label">코스피 종목 예시</span
                ><span class="hint">kospi_examples</span>
              </li>
              <li>
                <span class="step">08</span><span class="icon">❓</span
                ><span class="label">후속 질문 추천</span
                ><span class="hint">follow_up_q</span>
              </li>
            </ul>
          </div>

          <div class="chat-window">
            <div class="chat-header">
              <div class="dots"><span></span><span></span><span></span></div>
              <span>termcompass · live preview</span>
            </div>
            <div class="chat-body">
              <div class="user-msg">
                PER이 낮으면 사도 되나요? 업종별 기준이 궁금해요.
              </div>

              <div class="bot-msg">
                <div class="bot-section">
                  <div class="bot-section-title">📌 정의</div>
                  PER(주가수익비율) = 주가 ÷ EPS. 이익 대비 주가가 몇 배인지
                  보는 지표. 이익으로 투자금을 회수하는 데 걸리는 햇수의 근사치.
                </div>

                <div class="bot-section">
                  <div class="bot-section-title">🧮 수식</div>
                  <span class="formula">PER = 주가 ÷ EPS</span>
                </div>

                <div class="bot-section">
                  <div class="bot-section-title">💡 투자 활용법</div>
                  <ul>
                    <li>
                      코스피 평균 <span class="num">10~12배</span> / IT·바이오
                      <span class="num">20배+</span> / 은행·철강
                      <span class="num">5~8배</span>
                    </li>
                    <li>동일 업종 내 비교가 핵심 (절대값 비교는 무의미)</li>
                    <li>
                      <span class="num">10배 미만</span>은 저평가 후보지만 "이유
                      있는 저PER"인지 확인 필요
                    </li>
                  </ul>
                </div>

                <div class="bot-section">
                  <div class="bot-section-title">⚠️ 위험 경고</div>
                  <ul>
                    <li>PER 낮다고 무조건 매수 금물</li>
                    <li>
                      적자 전환 직전 / 일회성 이익으로 EPS 부풀려진 경우 PER
                      왜곡
                    </li>
                    <li>
                      같은 5배여도 사양 산업이면 함정, 회복 사이클이면 기회
                    </li>
                  </ul>
                </div>

                <div class="bot-section">
                  <div class="bot-section-title">🔄 관련 지표 비교</div>
                  PBR(자산 기준)과 함께. 성장주는 PER, 가치주는 PBR 위주.
                  <span class="formula">ROE × PER ≈ PBR</span>
                </div>

                <div class="bot-section">
                  <div class="bot-section-title">🎯 전문가 팁</div>
                  Forward PER(예상 PER)을 같이 보세요. 반도체 사이클 회복기엔
                  현재 <span class="num-blue">PER 30배</span>여도 1년 후 EPS
                  회복으로 Forward PER <span class="num-blue">10배대</span>로
                  떨어지는 경우가 많습니다.
                </div>

                <div class="bot-section">
                  <div class="bot-section-title">🏢 코스피 종목 예시</div>
                  삼성전자 · SK하이닉스 · 현대차 · 기아 · KT&amp;G
                </div>

                <div class="bot-section">
                  <div class="bot-section-title">
                    ❓ 다음에 물어볼 만한 질문
                  </div>
                  <div class="bot-followup">
                    <a href="#">PER과 PBR 중 뭘 봐야 하나요?</a>
                    <a href="#">업종마다 적정 PER이 다른가요?</a>
                    <a href="#">Forward PER은 어디서 확인하나요?</a>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- ==================== DATABASE ==================== -->
    <section>
      <div class="section-inner">
        <div class="section-header">
          <div class="section-header-left">
            <span class="section-num">03 / DATA</span>
            <h2 class="section-title">
              <em>Knowledge Base</em><br /><span class="kr"
                >데이터 기반, 환각 없음</span
              >
            </h2>
          </div>
          <p class="section-desc">
            검증된 144개 용어 KB에서 답변. 임의 종목·임의 수치를 만들어내지
            않습니다.
          </p>
        </div>

        <div class="db-grid">
          <div class="db-categories">
            <h4>category distribution</h4>
            <div class="cat-row">
              <span class="cat-name">시장 · 지수</span>
              <div class="cat-bar">
                <div class="cat-bar-fill" style="--w: 71%"></div>
              </div>
              <span class="cat-count">15</span>
            </div>
            <div class="cat-row">
              <span class="cat-name">기업가치 평가</span>
              <div class="cat-bar">
                <div class="cat-bar-fill" style="--w: 62%"></div>
              </div>
              <span class="cat-count">13</span>
            </div>
            <div class="cat-row">
              <span class="cat-name">재무제표</span>
              <div class="cat-bar">
                <div class="cat-bar-fill" style="--w: 90%"></div>
              </div>
              <span class="cat-count">19</span>
            </div>
            <div class="cat-row">
              <span class="cat-name">거래 · 주문</span>
              <div class="cat-bar">
                <div class="cat-bar-fill" style="--w: 100%"></div>
              </div>
              <span class="cat-count">21</span>
            </div>
            <div class="cat-row">
              <span class="cat-name">공시 · 이벤트</span>
              <div class="cat-bar">
                <div class="cat-bar-fill" style="--w: 90%"></div>
              </div>
              <span class="cat-count">19</span>
            </div>
            <div class="cat-row">
              <span class="cat-name">시장 제도</span>
              <div class="cat-bar">
                <div class="cat-bar-fill" style="--w: 81%"></div>
              </div>
              <span class="cat-count">17</span>
            </div>
            <div class="cat-row">
              <span class="cat-name">상품 유형</span>
              <div class="cat-bar">
                <div class="cat-bar-fill" style="--w: 95%"></div>
              </div>
              <span class="cat-count">20</span>
            </div>
            <div class="cat-row">
              <span class="cat-name">거시 지표</span>
              <div class="cat-bar">
                <div class="cat-bar-fill" style="--w: 71%"></div>
              </div>
              <span class="cat-count">15</span>
            </div>
          </div>

          <div class="db-columns">
            <h4>data columns (20)</h4>
            <div class="col-item">
              <div class="col-key">definition_easy</div>
              <div class="col-desc">
                쉬운 비유 — 어려운 용어를 일상 예시로 풀어줌
              </div>
            </div>
            <div class="col-item">
              <div class="col-key">
                investment_hint<span class="badge-new">v2</span>
              </div>
              <div class="col-desc">
                실제 투자 활용법 + 구체적 수치 기준 (PER 10~12배 등)
              </div>
            </div>
            <div class="col-item">
              <div class="col-key">
                risk_warning<span class="badge-new">v2</span>
              </div>
              <div class="col-desc">이 지표만 봤을 때의 함정과 위험 신호</div>
            </div>
            <div class="col-item">
              <div class="col-key">
                formula<span class="badge-new">v2</span>
              </div>
              <div class="col-desc">계산 수식 — 환각 없는 정확한 공식</div>
            </div>
            <div class="col-item">
              <div class="col-key">
                comparison_with<span class="badge-new">v2</span>
              </div>
              <div class="col-desc">관련 지표와의 비교 (PER vs PBR 등)</div>
            </div>
            <div class="col-item">
              <div class="col-key">
                expert_tips<span class="badge-new">v2</span>
              </div>
              <div class="col-desc">전문가 팁 — 실전 사례 기반의 조언</div>
            </div>
            <div class="col-item">
              <div class="col-key">
                kospi_examples<span class="badge-new">v2</span>
              </div>
              <div class="col-desc">
                실제 코스피 종목 예시 (검증된 데이터만)
              </div>
            </div>
            <div class="col-item">
              <div class="col-key">
                follow_up_q<span class="badge-new">v2</span>
              </div>
              <div class="col-desc">초보자가 자주 하는 후속 질문 3개</div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- ==================== SAMPLE QUESTIONS ==================== -->
    <section>
      <div class="section-inner">
        <div class="section-header">
          <div class="section-header-left">
            <span class="section-num">04 / TRY</span>
            <h2 class="section-title">
              <em>Ask</em> <span class="kr">이런 질문 해보세요</span>
            </h2>
          </div>
          <p class="section-desc">
            단순 정의 질문보다, 판단의 근거가 되는 응용 질문이 더 빛납니다.
          </p>
        </div>

        <div class="q-grid">
          <div class="q-card" onclick="openPanel()">
            <div class="q-tag">기업가치 평가</div>
            <div class="q-text">
              PER 10배면 저평가인가요? 업종별로 기준이 어떻게 다른가요?
            </div>
            <div class="q-arrow">↗</div>
          </div>
          <div class="q-card" onclick="openPanel()">
            <div class="q-tag">시장 제도</div>
            <div class="q-text">
              공매도 잔고 비율이 높은 종목은 왜 위험한가요?
            </div>
            <div class="q-arrow">↗</div>
          </div>
          <div class="q-card" onclick="openPanel()">
            <div class="q-tag">재무제표</div>
            <div class="q-text">
              PBR과 ROE를 같이 봐야 한다는데 어떻게 조합해 해석하나요?
            </div>
            <div class="q-arrow">↗</div>
          </div>
          <div class="q-card" onclick="openPanel()">
            <div class="q-tag">공시 · 이벤트</div>
            <div class="q-text">
              유상증자 공시는 호재인가요 악재인가요? 어떻게 구분하죠?
            </div>
            <div class="q-arrow">↗</div>
          </div>
          <div class="q-card" onclick="openPanel()">
            <div class="q-tag">거시 지표</div>
            <div class="q-text">
              기준금리 인하 시 어떤 업종이 수혜를 받나요?
            </div>
            <div class="q-arrow">↗</div>
          </div>
          <div class="q-card" onclick="openPanel()">
            <div class="q-tag">상품 유형</div>
            <div class="q-text">
              레버리지·인버스 ETF를 장기 보유하면 왜 손해가 누적되나요?
            </div>
            <div class="q-arrow">↗</div>
          </div>
        </div>
      </div>
    </section>

    <!-- ==================== HONESTY / FAQ ==================== -->
    <section class="honesty-section">
      <div class="section-inner">
        <div class="section-header">
          <div class="section-header-left">
            <span class="section-num">05 / TRUST</span>
            <h2 class="section-title">
              <em>Honest</em> <span class="kr">솔직하게 말씀드립니다</span>
            </h2>
          </div>
          <p class="section-desc">
            신뢰는 가능과 불가능을 명확히 구분하는 데서 시작합니다.
          </p>
        </div>

        <div class="faq-list">
          <div class="faq-item">
            <div class="faq-q">"종목 추천도 해주나요?"</div>
            <div class="faq-a">
              <span class="label-no">NO</span> "○○ 사세요" 같은 직접적 매수·매도
              추천은 하지 않습니다. 대신, 그 종목을 평가할 때 봐야 할
              <strong>지표와 관점</strong>을 제시합니다. 판단의 도구를 드리는
              것이 목적이지, 결정을 대신해드리지 않습니다.
            </div>
          </div>

          <div class="faq-item">
            <div class="faq-q">"내일 ○○ 오를까요?"</div>
            <div class="faq-a">
              <span class="label-no">NO</span> 단기 가격 예측은 불가능하며,
              시도하지도 않습니다. 대신 그 종목의 PER·PBR·재무건전성·공시 이벤트
              등 <strong>판단 재료</strong>를 함께 정리해드립니다.
            </div>
          </div>

          <div class="faq-item">
            <div class="faq-q">"다른 챗봇과 뭐가 다르죠?"</div>
            <div class="faq-a">
              <span class="label-yes">YES</span> 검증된 144개 용어 KB만 사용하여
              <strong>환각(hallucination)을 차단</strong>합니다. 임의 종목·임의
              수치를 지어내지 않으며, 모든 답변은 8개 섹션으로 정형화되어 있어
              빠짐없는 정보 전달이 가능합니다.
            </div>
          </div>

          <div class="faq-item">
            <div class="faq-q">"답변이 너무 길지 않을까요?"</div>
            <div class="faq-a">
              <span class="label-yes">YES</span> 8개 섹션 구조는
              <strong>역피라미드</strong> 방식입니다. 정의·수치 기준 같은
              핵심부터 먼저 읽고, 필요한 만큼만 아래로 내려가면 됩니다. 길이가
              아닌 <strong>밀도</strong>로 차별화됩니다.
            </div>
          </div>

          <div class="faq-item">
            <div class="faq-q">"투자 자문 자격이 있나요?"</div>
            <div class="faq-a">
              <span class="label-no">NO</span> 면허 받은 금융투자업자가
              아닙니다. 본 어시스턴트는 일반 정보 제공용이며, 모든
              <strong>투자 판단과 책임은 사용자 본인</strong>에게 있습니다.
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- ==================== FINAL CTA ==================== -->
    <section class="final-cta">
      <h2>
        <em>Now, your turn.</em><br /><span class="kr"
          >이제 질문을 던질 차례입니다.</span
        >
      </h2>
      <p>우측 하단의 챗봇 버튼으로 바로 시작할 수 있습니다.</p>
      <button class="btn-primary" onclick="openPanel()">
        챗봇 시작하기
        <svg width="16" height="16" viewBox="0 0 16 16" fill="none">
          <path
            d="M6 3l5 5-5 5"
            stroke="currentColor"
            stroke-width="2"
            stroke-linecap="round"
            stroke-linejoin="round"
          />
        </svg>
      </button>
    </section>

    <!-- ==================== FOOTER ==================== -->
    <footer>
      <div>© 2026 TermCompass</div>
      <div class="footer-right">
        <span>디지털콘텐츠디자인학과</span>
        <span>60221267</span>
        <span>김시현</span>
      </div>
    </footer>

    <!-- ==================== SIDE PANEL (chatbot iframe) ==================== -->
    <div class="side-panel" id="panel">
      <div class="resizer" id="resizer" title="드래그하여 크기 조절"></div>
      <iframe
        src="https://growfit.kr/agent/d2db39b4-8860-4def-a2a5-68365da96edb"
      ></iframe>
    </div>

    <button class="toggle-btn" id="toggleBtn">
      <span class="toggle-btn-icon"></span>
      <span id="toggleLabel">챗봇 열기</span>
    </button>

    <script>
      const panel = document.getElementById("panel");
      const toggleBtn = document.getElementById("toggleBtn");
      const toggleLabel = document.getElementById("toggleLabel");
      const resizer = document.getElementById("resizer");
      let isOpen = false;

      function openPanel() {
        if (!isOpen) {
          isOpen = true;
          panel.classList.add("open");
          document.body.classList.add("panel-open");
          toggleLabel.textContent = "닫기";
          toggleBtn.querySelector(".toggle-btn-icon").style.display = "none";
        }
      }

      function togglePanel() {
        isOpen = !isOpen;
        panel.classList.toggle("open");
        if (isOpen) {
          document.body.classList.add("panel-open");
          toggleLabel.textContent = "닫기";
          toggleBtn.querySelector(".toggle-btn-icon").style.display = "none";
        } else {
          document.body.classList.remove("panel-open");
          toggleLabel.textContent = "챗봇 열기";
          toggleBtn.querySelector(".toggle-btn-icon").style.display =
            "inline-block";
        }
      }

      toggleBtn.addEventListener("click", togglePanel);

      let isResizing = false;
      resizer.addEventListener("mousedown", () => {
        isResizing = true;
        document.body.style.cursor = "col-resize";
        document.body.style.userSelect = "none";
      });
      document.addEventListener("mousemove", (e) => {
        if (!isResizing) return;
        const newWidth = window.innerWidth - e.clientX;
        if (newWidth > 380 && newWidth < 800) {
          panel.style.width = newWidth + "px";
          document.documentElement.style.setProperty(
            "--panel-width",
            newWidth + "px",
          );
        }
      });
      document.addEventListener("mouseup", () => {
        if (isResizing) {
          isResizing = false;
          document.body.style.cursor = "";
          document.body.style.userSelect = "";
        }
      });

      document.querySelectorAll(".q-card").forEach((card) => {
        card.addEventListener("click", () => {
          const text = card.querySelector(".q-text").textContent.trim();
          if (navigator.clipboard) {
            navigator.clipboard.writeText(text).catch(() => {});
          }
          openPanel();
        });
      });

      const observer = new IntersectionObserver(
        (entries) => {
          entries.forEach((entry) => {
            if (entry.isIntersecting) {
              entry.target.classList.add("reveal");
              observer.unobserve(entry.target);
            }
          });
        },
        { threshold: 0.1, rootMargin: "0px 0px -80px 0px" },
      );

      document
        .querySelectorAll(
          ".section-header, .persona-card, .chat-window, .db-categories, .db-columns, .q-card, .faq-item",
        )
        .forEach((el) => {
          observer.observe(el);
        });
    </script>
  </body>
</html>
